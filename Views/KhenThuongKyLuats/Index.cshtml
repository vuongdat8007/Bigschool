@model IEnumerable<Bigschool_TH_11.Models.KhenThuongKyLuat>

@{
    ViewBag.Title = "Khen Thưởng / Kỷ Luật";
    ViewBag.ActiveMenu = "KhenThuongKyLuat";
    ViewBag.ActiveSub = "IndexKhenThuongKyLuat";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaKTKL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenLyDo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaKTKL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenLyDo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.MaKTKL }) |
            @Html.ActionLink("Details", "Details", new { id = item.MaKTKL }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.MaKTKL })
        </td>
    </tr>
}

</table>
